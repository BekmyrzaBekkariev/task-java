public class if13 {
}
/**Количество дней в месяце
 Напишите метод isLeapYear с параметром типа int с именем year.

 Параметр должен быть больше или равен 1 и меньше или равен 9999.

 Если параметр не находится в этом диапазоне, верните значение false.

 В противном случае, если оно находится в допустимом диапазоне, вычислите, является ли год високосным, и верните true, если это так, в противном случае верните false.

 Год считается високосным, если он делится на 4, но не на 100, или делится на 400.

 Примеры ввода/вывода:

 isLeapYear(-1600); → должно возвращать значение false, поскольку параметр не находится в диапазоне (1-9999)

 isLeapYear(1600); → должно возвращать значение true, поскольку 1600 - високосный год

 isLeapYear(2017); → должно возвращать значение false, поскольку 2017 год не является високосным

 isLeapYear(2000); → должно возвращать значение true, поскольку 2000 год - високосный

 ​ПРИМЕЧАНИЕ: Решение упражнения по кодированию високосного года, выполненного ранее в курсе, создало метод isLeapYear. Вы можете использовать это решение, если хотите.

 Напишите другой метод getDaysInMonth с двумя параметрами month и year. ​Оба типа int.

 Если параметр month равен < 1 или > 12, возвращает -1. ​

 Если параметр year равен < 1 или > 9999, то возвращает -1.

 Этот метод должен возвращать количество дней в месяце. Будьте осторожны с високосными годами, у них 29 дней во втором месяце (февраль).

 Вам следует проверить, является ли год високосным, используя метод isLeapYear, описанный выше.

 Примеры ввода/вывода:

 getDaysInMonth(1, 2020); → должно вернуться 31, так как в январе 31 день.

 getDaysInMonth(2, 2020); → должно вернуться значение 29, поскольку в феврале 29 дней в високосном году, а 2020 год является високосным.

 getDaysInMonth(2, 2018); → должно вернуться значение 28, поскольку в феврале 28 дней, если это не високосный год, а 2018 год не является високосным.

 getDaysInMonth(-1, 2020); → должен возвращать значение -1, поскольку параметр month недопустим.

 getDaysInMonth(1, -2020); → должен возвращать значение -1, поскольку параметр year находится за пределами диапазона от 1 до 9999.



 ПОДСКАЗКА: Используйте оператор switch.

 ПРИМЕЧАНИЕ: Методы isLeapYear и getDaysInMonth должны быть общедоступными статическими, как мы делали до сих пор в курсе.

 ПРИМЕЧАНИЕ: Не добавляйте основной метод в код решения. **/
 class NumberOfDaysInMonth {
	public static boolean isLeapYear(int year) {
		if (year < 1 || year > 9999) {
			return false;
		}

		if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {
			return true;
		}

		return false;
	}

	public static int getDaysInMonth(int month, int year) {
		if (month < 1 || month > 12 || year < 1 || year > 9999) {
			return -1;
		}

		switch (month) {
			case 1: case 3: case 5: case 7: case 8: case 10: case 12:
				return 31;
			case 4: case 6: case 9: case 11:
				return 30;
			case 2:
				if (isLeapYear(year)) {
					return 29;
				} else {
					return 28;
				}
			default:
				return -1;
		}
	}
}
